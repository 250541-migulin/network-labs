## 🚀 Быстрый старт

### Сборка
```bash
mvn clean package
```

## Запуск
Windows (Maven) → SCP → Хост (TFTP 172.20.200.40) → TFTP → Alpine VM в GNS3 (10.0.0.x)
1. скопировать на сервер tftp файлы jar папка /srv/tftp
2. на каждой машине есть скрипт update -> символическая ссылка на update.sh

## Как пользоваться в readme.md внутри лабораторной работы

## Изменение уровня логов в файле logback.xml

# Какие порты слушает сервер
netstat -tlnp

# Полное сканирование портов
nmap 10.0.0.2